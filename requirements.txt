# Sau pip install: python -m playwright install firefox  (hoặc playwright install)
# Video Editor / FFmpeg: ffmpeg PATH hoặc tools/ffmpeg/bin; ffplay — ưu tiên copy từ máy vào bin, không có mới tải zip full (tắt tải: TOOLFB_NO_AUTO_FFPLAY=1)
loguru>=0.7.0
playwright>=1.40.0
playwright-stealth>=1.0.0
pytest>=8.0.0
google-generativeai>=0.8.0
google-genai>=1.0.0
APScheduler>=3.10.0
streamlit>=1.32.0
requests>=2.31.0
